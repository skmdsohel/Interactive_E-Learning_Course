"""ORM models package.

Import every model module here so Alembic's autogenerate discovers
all tables via `Base.metadata`.
"""
# Add model imports here as the domain grows. Example:
# from app.models.user import User  # noqa: F401

__all__: list[str] = []
