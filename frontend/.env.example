# Base URL used by axios. In dev the Vite proxy forwards /api/* to the backend.
VITE_API_BASE_URL=/api/v1

# Target the dev-server proxy forwards /api requests to.
VITE_API_PROXY_TARGET=http://localhost:8000
